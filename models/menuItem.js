class MenuItem {
  constructor(id, name, icon, focusIcon, iconComponentName) {
    this.id = id;
    this.name = name;
    this.icon = icon;
    this.focusIcon = focusIcon;
    this.iconComponentName = iconComponentName;
  }
}

export default MenuItem;
